- [] build a page with the following support
    - list of checkboxes
    - list of radio buttons
    - autocomplete
    - window.alert, prompt and confirm
    - show and hide the element based on the button clicks
    - mouse hover
    - tables
    - select / dropdown
    - browser new tab opening (child window testing)
    - calendar selection
    - iframe
    - child window
    - new tab
    - loading fixtures
    - Class objects to split the test cases => `pageObjects`
    - custom cypress commands => `support > commands.js
    - env variables (passing cli command and config based)
    - opening the chrome using cli commands
    - report generator with `npm install -D cypress-mochawesome-reporter`
    - cypress cloud dashboard (report page)
    - retries (when fails or flaky results)