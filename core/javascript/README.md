# for dynamic build for ease use

```sh
bash ./compile.sh <folder_name>/<filename>.js
```
Note:

    - above filename extension should be `js` file where it will be target the `dist` folder which transpiled